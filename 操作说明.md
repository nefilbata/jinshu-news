# 金文速递操作说明

## 每日自动更新

GitHub Actions 已配置为每天北京时间 8:00 运行，也可以在 GitHub 的 Actions 页面手动点 `Run workflow`。

## 邮箱配置

在 GitHub 仓库的 `Settings -> Secrets and variables -> Actions` 添加：

| Secret | 说明 |
| --- | --- |
| `SMTP_HOST` | SMTP 服务器，例如 `smtp.qq.com`、`smtp.163.com` |
| `SMTP_PORT` | 常用 `465` 或 `587` |
| `SMTP_USER` | 发件邮箱账号 |
| `SMTP_PASS` | 邮箱授权码，不是登录密码 |
| `MAIL_FROM` | 发件人邮箱 |
| `MAIL_TO` | 收件人邮箱，多个用英文逗号分隔 |
| `AI_API_KEY` | DeepSeek 或兼容 OpenAI Chat Completions 的 API Key |

## 本地测试

```powershell
cd "D:\Desktop\make progress\jinshu-news\fetcher"
npm run dry
```

查看 `D:\Desktop\make progress\jinshu-news\docs\preview-email.html`。

## 重要说明

- 现在不再使用 Supabase。
- 历史数据保存在 `data/articles.json`。
- 每日日报保存在 `data/digests/`。
- 网页归档读取 `docs/data.json`。
- 某个信源失败不会阻断整封日报。
