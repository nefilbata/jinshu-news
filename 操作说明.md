# 金文动态 · 日常更新操作说明

---

## 每次更新（10分钟内完成）

### 第一步：抓取数据

打开 PowerShell，粘贴运行：

```powershell
cd "D:\Desktop\make progress\jinshu-news\fetcher"
$env:ANTHROPIC_API_KEY="你的DeepSeek Key"
$env:SUPABASE_URL="https://nhycnuxcrcwbrxmypshf.supabase.co"
$env:SUPABASE_SERVICE_KEY="你的Service Key"
node index.js
```

等待运行完毕，看到 `✅ 共抓取 XX 条` 即成功。

---

### 第二步：推送到 GitHub

```powershell
cd "D:\Desktop\make progress\jinshu-news"
git add .
git commit -m "data: 更新数据"
git pull --rebase
git push
```

网站会在1~2分钟内自动刷新。

---

## 建议更新频率

| 信源类型 | 更新频率 |
|---------|---------|
| 先秦史研究室、复旦、武大简帛 | 每周1次 |
| CNKI 期刊 RSS | 每周1次 |
| 公众号（RSSHub）| 每周1次 |
| CNKI 关键词搜索 | 每月1~2次 |

实际上**每周跑一次**就够，周末花10分钟操作即可。

---

## Key 轮换提醒

每次跑完建议轮换一次 Key，防止额度耗尽：

- DeepSeek Key：https://platform.deepseek.com
- Supabase Service Key：Supabase 控制台 → Project Settings → API

---

## 遇到冲突

push 报错时运行：

```powershell
git pull --rebase
git push
```

---

## 验证网站是否更新

打开 https://nefilbata.github.io/jinshu-news/

右上角显示的更新时间变成今天即成功。
